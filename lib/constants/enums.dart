enum NavigationDepth { one, two, three }

enum AnimationDirection { slideUp, slideDown, slideRight, slideLeft }

enum PopupMenuOption { summery, category }

enum HiveBoxKeys { category, summery, expenses, goal }


